# Sorcerer Troop (web)

## Run
```bash
npm install
npm run dev
```

Open `http://localhost:3000`.

## Google Places (optional)
Copy `web/.env.example` to `web/.env.local` and set:
`GOOGLE_PLACES_API_KEY=...`

Without a key, the app uses mock suggestions so the demo still works.

